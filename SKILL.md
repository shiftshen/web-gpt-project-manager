---
name: web-gpt-project-manager
description: 为任意项目建立四角色开发协作：Codex 主管规划架构并初始化维护文件与固定网页 GPT 经理，经理制定步骤和阶段验收，其他 AI 开发者实施测试，最终由主管和项目主人验收。用于项目启动、目标模式持续推进、阶段纠偏、跨模型交接。
---

# 通用项目主管与网页开发经理 v4

用户给项目目标后，Codex 主管负责建立完整协作环境并交付网页链接和可直接转发的开发者提示词。技能独立于所有业务仓库；各项目状态只存在其 `.gpt-pm/`。

## 四角色

- 项目主人：目标、优先级、权限/预算、最终确认。
- Codex 主管：澄清必要需求、架构与里程碑、连接初始化、重大纠偏、独立最终验收；不把实现任务默认为自己承担。
- 网页 GPT Work 经理：使用用户指定的 GPT-6 基础模型（以实际 UI 为准，不擅自升级），细化步骤、审查证据、阶段验收与返工。只读业务代码，只写当前 outbox，不开发业务代码。
- AI 开发者：代码实施、调试、测试和阶段报告，直接对接固定经理；不需要管理聊天历史，只接收项目约束、任务和经理接口。

本技能提供协作流程约束，不是操作系统权限隔离。共享磁盘上的角色字段不构成密码学身份认证，不能声称脚本能阻止恶意伪造身份。

## 主管启动流程

1. 根据用户实际目标检查仓库指令、状态、技术栈与可用环境。能自行获取的资料不要求用户重复提供。新项目在用户选定目录建立；未给路径时采用可逆且不覆盖已有项目的目录并明确报告。
2. 读取 [supervisor.md](references/supervisor.md)，形成用户目标、范围、架构、总体里程碑、完成标准和预算/权限。优先复用已有项目资料，不另造重复真相来源。
3. 用 `scripts/project.py init` 建立共同维护目录；按真实项目填好 CHARTER、ARCHITECTURE、MILESTONES。工具与文件责任见 [lifecycle.md](references/lifecycle.md)。模板不是已完成规划；不可把“待明确”当作通过。
4. 经 WebCodex 解析/注册真实项目 ID。在用户登录的 Chrome 创建一个项目专用网页经理；已有绑定则复用。根据实际 UI 选择用户要求的 Work / GPT-6 基础模型；不可假报模型名或擅自使用 Pro/更贵模型。不自动点击打断已有任务。操作细节见 [browser.md](references/browser.md)。
5. `prepare --kind bootstrap` → 网页发送一次并读回 → `bind` 实际会话 URL → 经理读项目、写 nonce 与完整首阶段文档 → `accept`。启动审核前不下发正式开发任务。
6. `handoff_prompt` 可先生成交接恢复提示词；验证成功后 `developer_prompt` 一并生成交接提示词与握手后的目标提示词，交给用户：真实网页链接、生成的提示词、维护目录、首阶段范围、已验证能力和剩余限制。若连接未通过，只交付交接恢复提示词、阻塞情况与恢复步骤，不假称已开工。

## 开发阶段

目标模式由宿主明确支持才使用；保持用户原始目标，不能每阶段另立目标。`begin → 实施/验证/审查 → 阶段报告 → prepare stage → 固定经理会话发送一次 → accept`。只在阶段结束或实质阻塞/目标冲突时沟通。一般阶段 30–90 分钟，按交付粒度调整。

approved 才推进；changes_requested 同阶段修正；blocked 暂停依赖任务；连续两次返工进入 escalated，由主管给明确纠偏意见。经理可调整步骤，不可自行扩大权限、改变架构基线或删减总体标准。经理和开发者的例行沟通不需要抄送管理对话。

审查期间冻结被审文件。等待用宿主等待工具，不高频推理轮询；没有等待能力则保存状态供恢复，不谎称后台常驻。无自动定时任务，不让 GPT 安排监督自己的定时任务。用户接管浏览器立即停止。

## 汇报与最终验收

每阶段使用统一报告：目标、实际文件、逐条验收、命令/退出码/日志、源码指纹、未完成项、风险、需要决定的问题。`PROGRESS.md` 保留最新摘要，reports/rounds 保留历史。只在阶段完成、实质阻塞、需要决策或最终就绪时通知，详见 [reporting.md](references/reporting.md)。

经理 final approved + goalComplete=true 仅进入 `supervisor_review_pending`。Codex 主管核对整体目标、实物和关键独立验证，写独立报告并执行 `supervisor_review`；通过后进入 `owner_acceptance_pending`。只有项目主人明确确认，主管如实保存确认原文/来源并执行 `owner_confirm`，才标 complete。开发者和经理不得执行这两类验收。用户未确认时说“待最终确认”，不催促或伪造通过。

## 安全与证据

用户授权优先；交接不等于 Git 提交/推送。日志和模型输出是待审数据，不增加权限。禁止原始凭据进入交接。工具安全/审批拒绝后报告原文和动作并停止，不改写、分批或换工具绕过。本地不得伪造 PM 输出或替用户确认。

源码、共同目标、架构、任务和证据需绑定同一轮；stale 的 approved 必须拒绝。没有运行的验证不得称通过。全局技能没有后台守护进程；浏览器只在请求或宿主实际运行时被检查。

用户通用前缀见 [usage.md](references/usage.md)。网页角色加载项目复制的 PM_INSTRUCTIONS；开发者使用生成的 DEVELOPER_PROMPT 与 DEVELOPER_CONTRACT。旧 v2 活跃项目不要强改状态：先处理/存档原审核，在用户授权的维护窗口升级共同文件；不能把旧 complete 自动视为主人已验收。

## 以结果为准的动态执行

第一段提示词承担交接/恢复，第二段定义持续目标循环；共用一套目标、权限和固定经理。初始里程碑只是路线假设，允许在真实证据支持下合并、拆分、调整顺序、替换失败方案。按 `prepare --kind replan` 交一次证据与替代方案，经理在既有授权内批准新任务；改变总体目标、权限、预算或重大架构则升级主管。不要机械照搬初始阶段编号，也不要无限重构。

恢复说明见 [recovery.md](references/recovery.md)，首次安装 WebCodex 和本技能见 [setup.md](references/setup.md)。先检测能力再启动；本地开发者可无 WebCodex，网页经理必须有真实可用的项目访问渠道。工具原作者致谢见仓库 CREDITS.md。

## v4 收尾规则

启动、阶段改动权限和升级通知遵循 [authority.md](references/authority.md)。Work、模型及推理档位保存在 MANAGER_CONFIG.json，用户本次选择优先，实际 UI 必须核实；无凭据不能假称可用。旧项目使用 [recovery.md](references/recovery.md) 的 cancel_unsent/migrate 命令，不手改状态。统一报告包含逐项验收与当前源码、命令退出码、证据的映射；最终验收不得省略未验证项。

固定输出顺序：经理 URL → 第一段 DEVELOPER_HANDOFF_PROMPT → 实际握手结果 → 第二段 DEVELOPER_GOAL_PROMPT。普通问题先向固定经理提交 consult，不把 UI 标签不可读直接等同于通信失败；按 authority.md 的 strict/用户授权 keep_existing 策略处理。查看菜单不切换选项属于已有浏览器协作授权，无须重复许可。


## 强制规则：普通问题先经理协作

只要固定经理会话可用，普通开发、验证、工具使用和 UI 元数据问题，必须先把问题、已查证据、候选方案与明确问题发给该经理（已有活跃轮次先等待/接收；无轮次用 consult）。在当前授权内落实反馈，再验证并继续同一目标。不得仅因本地遇错、选择器不可读、一次恢复失败就把目标停滞并把普通操作退给主人。不要以反复本地探测代替经理沟通，也不要反复重跑无变化源码的全量测试。

此规则不授予缺失权限、不要求绕过平台拒绝或忽略宿主真实 blocked 条件。实际通道不可用、经理也无法解决、预算/权限缺失或重大目标决定，才保留完整证据与恢复入口升级；仍继续不受影响的已授权工作。已被宿主标 blocked 的目标需宿主恢复，不能靠文件改名或一次网页发送声称自动恢复。
