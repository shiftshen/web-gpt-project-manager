---
name: web-gpt-project-manager
description: 为任意项目建立四角色开发协作：Codex 主管规划架构并初始化维护文件与固定网页 GPT 经理，经理制定步骤和阶段验收，其他 AI 开发者实施测试，最终由主管和项目主人验收。用于项目启动、目标模式持续推进、阶段纠偏、跨模型交接。
---

# 网页项目经理协作

为任意项目建立四角色流程：主人定目标与接受；主管规划/初始化/重大纠偏/独立终验；网页经理负责阶段决策、证据核验和验收；本地程序员负责代码实现、测试、留证和持续推进。启动时先按 [roles.md](references/roles.md) 明确能力、职责和回答风格。

## 启动与固定交付

1. 检查真实仓库指令、状态、授权和目标，不要求重复上传本机资料。按 [supervisor.md](references/supervisor.md) 完成章程/架构/动态里程碑。
2. `project.py init` 创建 .gpt-pm；填真实内容，勿保留空模板冒充规划。旧 v2/v3 用 [迁移命令](references/recovery.md)，不手改状态、不覆盖活跃轮次。
3. 创建或复用用户登录浏览器中的唯一固定经理；角色指令由 PM_INSTRUCTIONS 在 bootstrap 加载。先验证 WebCodex；配置入口和原作者致谢见 [setup.md](references/setup.md)。
4. 模式/模型/档位分别记录 requested/observed。strict 遵守指定要求；有真实用户授权的 keep_existing 可保留 unknown 沿用当前会话，不切换、不升级。详见 [authority.md](references/authority.md)。
5. 首次 bootstrap 通过真实文件 nonce + 完整回传 + 新鲜快照验证。已有有效握手直接复用；普通技能更新不重新握手。目标、模型策略或经理更换影响门槛时才按协议恢复。
6. 固定交付：经理 URL → 第一段交接提示词 → 实际握手结果 → 第二段目标提示词。handoff_prompt 可先签发恢复提示；developer_prompt 只在握手有效且状态允许时输出两段文件。不得假称未完成的握手已通过。

## 经理负责管理与验证

经理默认中文、结论优先，通常 200–500 字：理由、最多三项下一步、验收条件。日常先读简短场景和证据摘要，必要时看小片段。关键疑难时给程序员方案、最小试验和验收条件；程序员负责代码实施。经理可按需读文件或运行聚焦验证，只写协议所需的审核记录或握手文件，不改业务代码。详见 [roles.md](references/roles.md)。

默认阶段/replan/final 使用 [chat-review.md](references/chat-review.md)：网页直接回复，本地抓取原文、核对会话/轮次/源码，再 accept 同步计划。首次 bootstrap 或显式 files 才走 outbox。不能把本地转录说成经理通过 WebCodex 写文件。

## 强制问题处理与等待

普通错误先本地定位并做最小修复；涉及方案、验收、权限或无法自行解决的问题，再给固定经理简短场景、进度、一个问题和最多两个方案。落实反馈后继续原目标，不仅因本地错误、UI 标签不可读或回复慢就停目标。未握手也可 consult 诊断，但诊断不批准开发。普通问答直接发给经理并保留关键原文，不必 prepare 或冻结源码；正式阶段验收才走审核门槛。

重要问题可连续短问答，明显误解、漏关键限制或接管研发时允许用户已授权的必要打断；正常处理耐心等，不盲目重发。使用宿主等待能力，通常 60–120 秒观察一次，单次调用不超过 60 秒；没有等待/唤醒能力如实保存现场，不伪造常驻。详见 [communication.md](references/communication.md)。

源文件在阶段审查期间冻结；stale 批准拒绝。程序员执行测试，充分且同源码的结果复用，不因等待反复全量验证。每项验收对应源码指纹、命令/退出码、证据路径与结果，报告格式见 [reporting.md](references/reporting.md)。

## 自主边界与终验

经理可以重排/拆分阶段、补测试、在架构内选方案；技术栈、数据格式、核心接口、范围、最终标准或权限/预算变化升级主管。真正通道不可用、缺外部授权或经理无法解决的重大事项才升级；保留恢复入口，继续不受影响的授权工作。写 ESCALATIONS 不等于通知已送达。

经理 final approved + goalComplete=true 仅是 supervisor_review_pending；主管独立验收通过后等主人明确接受才 complete。程序员不执行主管/主人验收命令。宿主目标的 blocked/恢复按实际宿主规则，不以共享文件冒充状态。

平台拒绝不得改写/拆分/换工具绕过。共享文件中的角色和原文哈希是协作证据，不是密码学身份认证，也不提供 OS 隔离。未知模型不声称已核实，无免费额度或节省额度承诺。

完整命令见 [lifecycle.md](references/lifecycle.md)，用户前缀和两段交付见 [usage.md](references/usage.md)。
